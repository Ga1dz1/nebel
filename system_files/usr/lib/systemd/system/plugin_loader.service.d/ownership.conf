[Service]
# Decky and its updaters unpack plugin files as root; hot-reload and the
# frontend build watcher need the tree user-owned. Normalize ownership after
# every loader (re)start, unconditionally.
ExecStartPost=/usr/bin/chown -R nebel:nebel /var/home/nebel/homebrew
