# Game mode (gamescope session) has no KDE session running; routing portal
# requests to the KDE backend there made plasma-xdg-desktop-portal-kde get
# dbus-activated against a missing display and crash-loop. Use the GTK backend
# for the gamescope desktop instead.
[preferred]
default=gtk
