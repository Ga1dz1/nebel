# Allow the armada user to toggle the shared-storage mount without a password.
armada ALL=(root) NOPASSWD: /usr/libexec/nebel/shared-storage-setup enable, /usr/libexec/nebel/shared-storage-setup disable
